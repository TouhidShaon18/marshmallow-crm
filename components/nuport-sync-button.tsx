// Polling-based sync removed — integration now uses webhooks.
// See /api/webhooks/nuport for the receiver and /settings for setup instructions.
export default function NuportSyncButton() { return null; }
